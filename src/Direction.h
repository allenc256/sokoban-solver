#pragma once

enum class Direction { UP = 0, DOWN = 1, LEFT = 2, RIGHT = 3 };

constexpr Direction ALL_DIRECTIONS[] = {Direction::UP, Direction::DOWN,
                                        Direction::LEFT, Direction::RIGHT};
